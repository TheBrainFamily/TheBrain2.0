// @flow
import { StyleSheet } from 'react-native'

export default StyleSheet.create({
  headerLogo: {
    padding: 0,
    marginLeft: -25,
    marginTop: 10,
    marginBottom: 0,
    flex: 1,
    transform: [
      {scale: 1.2}
    ]
  }
})
